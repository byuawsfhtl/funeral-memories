import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.funeralmemories.fhtl",
  appName: "Funeral Memories",
  webDir: "dist",
};

export default config;
